{
  "name": "Karaoke Time",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "postCreateCommand": "chmod +x ./initialize_environment.sh && bash ./initialize_environment.sh",

  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "KaraokeTime",
        "terminal.integrated.profiles.linux": {
          "KaraokeTime": {
            "path": "/bin/zsh",
            "args": [
              "-i",
              "-c",
              "cd /workspaces/${localWorkspaceFolderBasename} && source demucs_env/bin/activate && exec zsh -i"
            ]
          }
        }
      }
    }
  }
}
